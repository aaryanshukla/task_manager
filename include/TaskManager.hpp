// sets up task struct/class 
#include <string>

namespace std { 
    struct Tasks {
    // what do we need 

    // task name (String): 

    string task_name;


    // description of task: string

    string task_description;

    // completion of task: bool 

    bool task_complete;
    }  
} 